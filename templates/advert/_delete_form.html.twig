<form method="POST" action="{{ path('app_advert_delete', {'id': advert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this advert?\nThis action is irreversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ advert.id) }}">
    <button>Delete</button>
</form>
